<?xml version='1.0' encoding='utf-8'?>
<section xmlns="http://docbook.org/ns/docbook" xml:id="argument-from-ignorance">
  <title>The Argument from Ignorance: Why Existing Legal Defences Collapse</title>
  <para>Courts, legislators, and AI companies have constructed a set of legal defences premised on the proposition that LLM weights are <quote>abstract mathematical transformations</quote> — not databases, not records, not structured stores of retrievable data. That proposition was, in 2023 and 2024, difficult to refute empirically. In 2026, after LARQL's public release and the empirical demonstrations described in this paper, it is false on its face.</para>
  <para>The defences share a common logical structure: they are <emphasis>arguments from ignorance</emphasis>. They assert that, because no one has demonstrated a working query interface to the weights, the weights are not queryable — that, because no CRUD tool existed for weights, the weights have no CRUD-semantics. LARQL eliminates that ignorance. The argument form collapses when the premise is refuted by empirical demonstration. Courts and LLMs alike that have not been trained on the 2026 empirical record are, on this specific point, operating with materially incomplete information.</para>
  <table frame="all" rowsep="1" colsep="1">
    <title>Four Refuted Legal Defence Claims (2026)</title>
    <tgroup cols="3">
      <colspec colname="c1" colwidth="1.4*" />
      <colspec colname="c2" colwidth="1.8*" />
      <colspec colname="c3" colwidth="2.2*" />
      <thead>
        <row>
          <entry>Legal Defence Claim</entry>
          <entry>Ignorance Presumption</entry>
          <entry>LARQL and Empirical Reality (2026)</entry>
        </row>
      </thead>
      <tbody>
        <row>
          <entry>
            <emphasis role="bold">GDPR Art. 17 Inapplicability</emphasis>
          </entry>
          <entry>Data ceases to exist in identifiable form after mathematical transformation into weights.</entry>
          <entry>Weights are geometric addresses. Original text and images are reconstructible with high fidelity by pumping the model with a known subgraph (e.g. Wikidata). INSERT INTO EDGES demonstrates that named personal facts are writable and deletable at specific (layer, feature) coordinates. The claim of non-identifiability is demonstrably false.</entry>
        </row>
        <row>
          <entry>
            <emphasis role="bold">Database Directive Mismatch</emphasis>
          </entry>
          <entry>Model weights are unique algorithmic outputs, not a structural compilation individually accessible by electronic means.</entry>
          <entry>A KNN walk over the FFN layers and a dense matrix multiply are proven to produce identical predictions at every layer boundary. The walk reads from a memory-mapped file (down_features.bin) — a physically existing database file. Individual entries are addressable by (layer, feature) coordinates. The weights ARE a structural compilation individually accessible by electronic means.</entry>
        </row>
        <row>
          <entry>
            <emphasis role="bold">Linear-Combination-as-Copy Defence</emphasis>
          </entry>
          <entry>Non-linear activation functions (ReLU, GELU) destroy similarity and make copyright tracing impossible.</entry>
          <entry>The algebraic fallacy: linear combinations are exactly how non-linear expressions are constructed (as binomials are constructed from monomials). The float matrices represent an explicit spatial arrangement of concepts. Once the coordinate geometry is decoded via a known dictionary, the underlying expressive content appears as a high-fidelity duplicate. The FFN output is a linear combination of stored value vectors — non-linearity determines which vectors are selected, not whether they are stored or retrievable.</entry>
        </row>
        <row>
          <entry>
            <emphasis role="bold">Obfuscation Irrelevance</emphasis>
          </entry>
          <entry>Weights are an emergent black box, not a deliberate encoding of training content. The <quote>only numbers</quote> claim.</entry>
          <entry>Programming code and structured graphs do not spontaneously scramble into random weights. The training pipeline, tokenisation layers, and weight matrices were designed by engineers who know they are creating a compressed graph database. The <quote>black box</quote> is a legal shield, not a mathematical absolute. The transformation from training data to weights is deterministic and invertible at the level of the factual knowledge encoded therein — as LARQL demonstrates.</entry>
        </row>
      </tbody>
    </tgroup>
  </table>
  <section xml:id="holographic-reconstruction">
    <title>Holographic Reconstruction and the Identifiability of Weight-Encoded Data</title>
    <para>The model's weights act as a holographic projection of its training corpus in the following precise sense: every fragment of the weight tensor contains information about the global distribution of training content, because the gradient updates during training propagate information from any given training example across the entire weight matrix (albeit with diminishing influence at greater distance in parameter space). Like an optical hologram, where each fragment contains a degraded but complete image of the whole, a model's weights contain a degraded but complete encoding of each training-data item that was memorised with sufficient frequency or weight.</para>
    <para>LARQL demonstrates this constructively: by injecting a known subgraph (e.g. Wikidata entity–relation–target triples) into the model via forward-pass residual capture, a practitioner can build a decoder that maps the model's internal feature coordinates to external world concepts, effectively de-obfuscating the geometric encoding. The transformation-theory specification in the LARQL codebase formalises this as a coupling calculus: <command>E(M) ∩ K</command>, the intersection of the model's internal knowledge graph <command>E(M)</command> (obtained by forward-passing relation templates) and an external knowledge graph <command>K</command>. This intersection is the grounded, verifiable record of what the model stores from its training corpus.</para>
  </section>
</section>

  
  
  