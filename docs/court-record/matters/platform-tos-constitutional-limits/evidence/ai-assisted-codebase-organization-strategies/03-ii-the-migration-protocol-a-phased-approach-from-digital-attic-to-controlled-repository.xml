<?xml version='1.0' encoding='utf-8'?>
<section xmlns="http://docbook.org/ns/docbook" xml:id="ii-the-migration-protocol-a-phased-approach-from-digital-attic-to-controlled-repository">
  <title>
    <emphasis role="strong">II. The Migration Protocol: A Phased
    Approach from Digital Attic to Controlled
    Repository</emphasis>
  </title>
  <para>
      With the foundational architecture defined, the next phase
      involves the methodical and disciplined ingestion of the user's
      unversioned corpus into the newly structured monorepo. This
      process is not a simple file copy; it is a strategic operation
      designed to establish a clean, functional, and performant baseline
      for all future automated work. A pragmatic approach that
      prioritizes a stable starting point over a perfectly reconstructed
      history is paramount.
    </para>
  <section xml:id="s-21-the-tip-migration-philosophy-prioritizing-a-clean-slate-over-reconstructed-history">
    <title>
      <emphasis role="strong">2.1. The "Tip Migration"
      Philosophy: Prioritizing a Clean Slate over Reconstructed
      History</emphasis>
    </title>
    <para>
        Given that the existing corpus of projects lacks a formal
        version control history, any attempt to retroactively create one
        would be a time-consuming and speculative exercise with a very
        low return on investment. The recommended strategy is a
        <emphasis role="strong">"tip migration"</emphasis>,
        which involves migrating only the most recent version (the
        "tip") of each project's files into the new Git
        repository.17
      </para>
    <para>
        This approach deliberately forgoes a detailed commit history in
        favor of establishing a single, clean, and functional initial
        commit. This baseline becomes the definitive starting point from
        which all future changes will be tracked with the rigor of
        version control. If historical context is ever needed, the
        user's original file structure can be preserved as a read-only,
        offline archive.17 The primary goal of the migration is not to
        document the past, but to enable a more efficient and automated
        future.
      </para>
  </section>
  <section xml:id="s-22-pre-flight-checklist-preparing-for-ingestion">
    <title>
      <emphasis role="strong">2.2. Pre-Flight Checklist:
      Preparing for Ingestion</emphasis>
    </title>
    <para>
        Before any files are moved, a pre-flight check is necessary to
        identify and correctly handle assets that could compromise the
        health and performance of the Git repository.
      </para>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          <emphasis role="strong">Identify and Manage Large/Binary
            Files:</emphasis> Git is optimized for managing text-based
            source code; it performs poorly when large binary files are
            committed directly to its history. A scan of the corpus must
            be performed to identify all such assets. This includes
            datasets, high-resolution images, compiled artifacts, and
            potentially large PDF outputs from the LaTeX projects. These
            files must be managed using <emphasis role="strong">Git
            Large File Storage (LFS)</emphasis>. Git LFS works by
            storing these large files on a separate server while keeping
            lightweight pointers within the Git repository itself,
            ensuring that cloning and other Git operations remain
            fast.20
          </para>
      </listitem>
      <listitem>
        <para>
          <emphasis role="strong">Exclude Dependencies and
            Executables:</emphasis> The repository should contain only
            source code and essential assets, not the dependencies
            required to build or run them. All checked-in dependency
            folders (e.g., node_modules for JavaScript, venv or
            __pycache__ for Python) and compiled executables must be
            removed. These should be managed by their respective package
            managers (npm, pip) and regenerated on demand, not stored in
            version control.18
          </para>
      </listitem>
      <listitem>
        <para>
          <emphasis role="strong">Establish a Comprehensive
            .gitignore:</emphasis> A robust .gitignore file should be
            created at the root of the monorepo before the first commit.
            This file will instruct Git to ignore common build
            artifacts, IDE configuration files, operating system files,
            and the dependency directories mentioned above for all
            languages present in the codebase. This prevents accidental
            commits of transient or machine-specific files.
          </para>
      </listitem>
    </itemizedlist>
  </section>
  <section xml:id="s-23-the-migration-workflow-a-step-by-step-execution-plan">
    <title>
      <emphasis role="strong">2.3. The Migration Workflow: A
      Step-by-Step Execution Plan</emphasis>
    </title>
    <para>
        The migration should be conducted as a structured, repeatable
        process to ensure consistency and create a stable foundation.
      </para>
    <orderedlist numeration="arabic" spacing="compact">
      <listitem>
        <para>
          <emphasis role="strong">Initialize the Repository and
            Tooling:</emphasis> Create a new directory, initialize it as
            a Git repository (git init), and set up the chosen monorepo
            management tool (e.g., npx create-nx-workspace@latest). This
            will create the initial configuration files for the tool.
          </para>
      </listitem>
      <listitem>
        <para>
          <emphasis role="strong">Establish the Directory
            Structure:</emphasis> Create the top-level directories
            defined in the architectural blueprint: /apps, /packages,
            /docs, and /tools.
          </para>
      </listitem>
      <listitem>
        <para>
          <emphasis role="strong">Configure Git LFS:</emphasis>
            Install and initialize Git LFS for the repository (git lfs
            install) and configure it to track the file types identified
            in the pre-flight check (e.g., git lfs track
            "*.pdf").
          </para>
      </listitem>
      <listitem>
        <para>
          <emphasis role="strong">Iterative Project
            Ingestion:</emphasis> This is the most critical manual step
            of the entire process. One by one, copy each project from
            the legacy corpus into its appropriate destination within
            the monorepo structure (e.g., a React application goes into
            /apps/my-react-app, a shared Python library goes into
            /packages/my-python-lib).
          </para>
      </listitem>
      <listitem>
        <para>
          <emphasis role="strong">Create the Initial
            Commit:</emphasis> Once all projects have been placed, stage
            all the new files and create a single, comprehensive initial
            commit. The commit message should be clear and descriptive,
            such as "feat: Initial migration of legacy polyglot
            codebase". This action establishes the main branch and
            creates the foundational, stable state of the repository.
          </para>
      </listitem>
      <listitem>
        <para>
          <emphasis role="strong">Define a Branching
            Strategy:</emphasis> For all subsequent work, a simple yet
            robust branching strategy should be adopted.
            <emphasis role="strong">GitHub Flow</emphasis> is highly
            recommended. In this model, the main branch is always stable
            and deployable. All new work, whether performed by a human
            or an AI agent, is done on short-lived feature branches
            created from main. Once complete, these branches are merged
            back into main via a pull request.18
          </para>
      </listitem>
    </orderedlist>
    <para>
        The manual effort expended during this migration phase,
        specifically the act of sorting projects into the /apps and
        /packages directories, is not merely organizational hygiene. It
        is the first and most fundamental act of imposing a high-level
        structure that the AI agent will later leverage. An AI agent
        like Jules begins with no intrinsic understanding of the
        codebase's intent. If presented with a flat directory of
        projects, it would be forced to infer all architectural
        relationships from scratch—a complex and error-prone task. By
        placing a project in /packages/shared-ui, the user provides a
        powerful and explicit piece of metadata: "This code is
        intended to be a reusable library for user interfaces."
        Later, when Jules is prompted to "build a new user profile
        page in the main application," it can infer that it should
        utilize components from /packages/shared-ui rather than
        inventing new ones. This initial, manual classification
        bootstraps the AI's understanding, dramatically reducing
        ambiguity and improving the quality and relevance of its
        subsequent automated actions.
      </para>
  </section>
</section>
  