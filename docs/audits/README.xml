<?xml version="1.0" encoding="UTF-8"?>
<article xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.2" xml:id="readme" xml:lang="en">
  <xi:include href="README.meta.xml" />
  <para><code>footnote-citation-audit.csv</code> is generated by <code>docs/scripts/audit_footnote_links.py</code>. It is a
  <emphasis role="strong">read-only report</emphasis> — no corpus document is modified by generating it. See
  <link xlink:href="../superpowers/specs/2026-07-23-footnote-citation-audit-design.md">the design spec</link> for the
  full rationale, and <link xlink:href="../superpowers/plans/2026-07-23-footnote-citation-audit-plan.md">the implementation plan</link>
  for how it was built.</para>

  <para>Each row is a candidate footnote-number marker matched positionally to a works-cited entry, with
  a confidence tier (<code>High</code> / <code>Medium</code> / <code>Needs manual triage</code>) and a <code>flags</code> column explaining why.
  Regenerate with <code>python3 docs/scripts/audit_footnote_links.py</code> any time the corpus changes.</para>

  <para><emphasis role="strong">Current run:</emphasis> 7,667 candidates — 6,105 High, 1,025 Medium, 537 Needs manual triage — across 95
  documents. The original 6,129/1,025/513 split was verified against real content by a full
  human-review pass (all 40 <code>restart_detected</code> rows as they stood then, 30 <code>High</code>, 20 <code>Medium</code>, and
  a stratified 30-row sample of <code>Needs manual triage</code>), before a critical whole-branch-review fix
  (per-fragment restart detection — restart detection now runs independently on each fragment file
  rather than on the whole document's concatenated candidate sequence, per design spec §6; see
  <code>.superpowers/sdd/task-11-report.md</code>) corrected <code>restart_detected</code> from 40 rows across 6 documents
  to 88 rows across 11 documents. Most notably, <code>the-architecture-of-non-consensual-legality.html</code>
  had a genuine restart entirely confined to one fragment that the prior whole-document check missed
  completely (0 <code>restart_detected</code> rows) because a <emphasis>later, unrelated</emphasis> fragment's own numbering
  happened to climb back near the pre-drop value — 14 candidates that should have been <code>Medium</code>
  shipped as confidently-wrong <code>High</code> rows. The fix moved 24 candidates out of <code>High</code> net
  corpus-wide (14 of them in this document alone); the rest of the corpus's <code>Medium</code>/<code>Needs manual
  triage</code> counts also shifted (some rows moved the other way, into or out of <code>Needs manual
  triage</code>, as previously-merged fragments were re-scored independently) — see the fix report for the full
  per-document before/after breakdown. These changes are the direct, verified-correct effect of the
  fix (confirmed via new regression tests plus a re-run of the full test suite); they have not been
  independently re-reviewed row-by-row by a human beyond that automated verification.</para>

  <para><emphasis role="strong">Two known, honestly-labeled data-quality caveats found during that review</emphasis> (corpus defects the
  audit correctly surfaces, not audit bugs — see design spec §9, "explicitly sequenced follow-on
  work," for the broader cleanup this belongs to):</para>

  <orderedlist numeration="arabic">
    <listitem>
      <para><emphasis role="strong">~32 <code>High</code>-confidence rows have thin entry text</emphasis> (<code>", accessed &lt;date&gt;, "</code> plus a bare
      domain like <code>sec.gov</code>/<code>irs.gov</code>) because the <emphasis>source</emphasis> works-cited entry itself is malformed —
      a citation with a real, resolvable link but no descriptive title. The <code>High</code> tier is correct
      (the link corroboration is real), but a human triager will need to open the source document to
      see what's actually cited.</para>
    </listitem>
    <listitem>
      <para><emphasis role="strong"><code>contractual-architectures-for-ip-and-cooperatives.html</code>'s works-cited list contains ~3
      entries that are LLM-prompt-fragment text</emphasis>, not real citations (corpus pollution from an
      earlier authoring/conversion step). The audit routes these to <code>Needs manual triage</code>/<code>Medium</code>
      via the missing-link flag, but doesn't specifically distinguish "this isn't a citation at all"
      from an ordinary missing link — a human triager will recognize them immediately on inspection.</para>
    </listitem>
  </orderedlist>

  <para><emphasis role="strong">This report identifies candidates for a future footnote-to-citation linking pass — it does not
  perform one.</emphasis> Any mechanical rewrite of the corpus's 446 files based on this report's <code>High</code> rows
  is a separate, not-yet-designed project (design spec §9).</para>
</article>
